
@model ProductVM

<div class="container">
    <div class="row mb-3 align-items-center my-3">
        <div class="col-md-6">
            <h2 class="display-6">update Product</h2>
        </div>
    </div>

    <div class="row my-3">
        <div class="col-12">
            <form method="post" enctype="multipart/form-data">
                <input class="form-control"  hidden name="Id" value="@Model.Product.Id">
                <div class="mb-3">
                    <label for="name" class="form-label">Name</label>
                    <input class="form-control" name="Name" value="@Model.Product.Name">
                </div>
                <div class="mb-3">
                    <label for="MainImg" class="form-label">MainImg</label>
                    <input type="file" class="form-control" name="img" accept="image/*" onchange="previewImage(event)">
                </div>
                <div class="mb-3">
                    <img id="preview"
                         src="~/images/@Model.Product.MainImg"
                         alt="Current Image"
                         style="max-width: 200px; border:1px solid #ccc; padding:5px; border-radius:10px;" />
                </div>
                <div class="mb-3">
                    <label for="SubImages" class="form-label">SubImages</label>
                    <input type="file" class="form-control" name="SubImages" multiple accept="image/*" ">
                </div>
                <div class="mb-3 d-flex ">
                    @foreach(var item in Model.Product.ProductSubImages)
                    {
                        <div style="position:relative;">
                            <img id="preview"
                                src="~/images/product_sub_images/@item.Img"
                                alt="Current Image"
                                style="max-width: 200px; border:1px solid #ccc; padding:5px; border-radius:10px;" />
                            <a  href="/Admin/Product/DeleteSubImage?productId=@item.ProductId&img=@item.Img" class="btn btn-danger" style="position:absolute; top:0; right:0; border-radius:50%; ">x</a>
                        </div>
                    }
                </div>

                
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <div class="form-floating">
                        <textarea class="form-control" placeholder="Leave a description here" style="height: 100px" name="Description">@Model.Product.Description
                        </textarea>
                        <label for="description">You Description Here...</label>
                    </div>
                </div>
                <div class="mb-3">
                    <label for="Price" class="form-label">Price</label>
                    <input class="form-control" name="Price" type="number" value="@Model.Product.Price">
                </div>
                <div class="mb-3">
                    <label for="Quantity" class="form-label">Quantity</label>
                    <input class="form-control" name="Quantity" type="number" value="@Model.Product.Quantity">
                </div>
                <div class="mb-3">
                    <label for="Discount" class="form-label">Discount</label>
                    <input class="form-control" name="Discount" type="number" value="@Model.Product.Discount">
                </div>
                <div class="mb-3">
                    <label for="Color" class="form-label">Color</label>

                    <div id="colorContainer">
                        @foreach (var item in Model.Product.ProductColors)
                        {
                            <div class="mb-2 d-flex align-item-center">
                                <input type="color" class="form-control form-control-color" name="Colors" value="@item.Color">
                                <button type="Button" class="btn btn-danger btn-sm removeColor">x</button>
                            </div>
                        }
                    </div>

                    <button type="button" id="addButtonColor" class="btn btn-outline-primary">+ Add New Color</button>
                </div>
                <div class="mb-3">
                    <label for="CategoryId" class="form-label">Categories</label>
                    <select name="CategoryId" class="form-control">
                        <option>--Select Category--</option>
                        @foreach (var item in Model.Categories)
                        {
                            @if (Model.Product.CategoryId == item.Id)
                            {
                                <option value="@item.Id" selected> @item.Name</option>
                                continue;
                            }
                            <option value="@item.Id"> @item.Name</option>
                        }
                    </select>
                </div>

                <!-- Brand -->
                <div class="mb-3">
                    <label for="BrandId" class="form-label">Brands</label>
                    <select name="BrandId" class="form-control">
                        <option>--Select Brands--</option>
                        @foreach (var item in Model.Brands)
                        {
                            @if (Model.Product.BrandId == item.Id)
                            {
                                <option value="@item.Id" selected> @item.Name</option>
                                continue;
                            }
                            <option value="@item.Id"> @item.Name</option>
                        }

                    </select>
                </div>
                <div class="mb-3">
                    <label for="status" class="form-label">Status</label>
                    <select name="Status" class="form-select">
                        @if(Model.Product.Status)
                        {   
                            <option value="true" selected>Active</option>
                            <option value="false">InActive</option>
                        }
                        else
                        {
                            <option value="true">Active</option>
                            <option value="false" selected>InActive</option>   
                        }

                    </select>
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
    </div>
</div>


<script>
    function previewImage(event) {
        const input = event.target;
        const preview = document.getElementById("preview");

        if (input.files && input.files[0]) {
            const reader = new FileReader();
            reader.onload = function(e) {
                preview.src = e.target.result; // الصورة الجديدة
            }
            reader.readAsDataURL(input.files[0]);
        }
    }
</script>

<script>
    const conateiner = document.getElementById('colorContainer');
    const addBtn = document.getElementById('addButtonColor');

    addBtn.addEventListener('click', () => {
        var colorRow = document.createElement('div');

        colorRow.className = 'mb-2 d-flex align-item-center gap-1'
        colorRow.innerHTML = `
            <input type="color" class="form-control form-control-color" name="Colors">
            <button type="Button" class="btn btn-danger btn-sm removeColor">x</button>
        `;

        conateiner.appendChild(colorRow);
    });

    conateiner.addEventListener('click', (e) => {
        if(e.target.classList.contains('removeColor')) {
            e.target.parentElement.remove();
        }
    })
</script>